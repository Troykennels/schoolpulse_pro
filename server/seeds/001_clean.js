/**
 * SchoolPulse v2.1 — Clean Seed
 * 
 * This seed file is intentionally empty.
 * The system uses a first-time setup flow (/api/auth/setup) to create
 * the initial school and super admin account.
 * 
 * All data is entered by the school administrator through the UI.
 * No sample/demo data is pre-loaded.
 */
exports.seed = async function (knex) {
  // Intentionally empty — clean system
  console.log('SchoolPulse database ready. Use the setup wizard to create your school.');
};
